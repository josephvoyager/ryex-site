import PageStub from '@/components/app/PageStub';

export default function LiquidationPage() {
  return (
    <PageStub
      title="Liquidation"
      oneLiner="RLT redemption queue. FIFO ordering. Redeem rTokens against the most at-risk vaults at oracle price."
    />
  );
}
